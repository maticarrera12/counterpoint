<section id="manifiesto" style="padding: clamp(56px, 10vw, 150px) clamp(16px, 4vw, 56px); position: relative;">
  <div style="display: grid; grid-template-columns: repeat(12, minmax(0, 1fr)); gap: 0 clamp(8px, 1.6vw, 22px);">
    <div style="grid-column: 1 / 2; font-size: 11px; letter-spacing: 0.14em; text-transform: uppercase; font-weight: 600;">02</div>
    <div style="grid-column: 2 / 5; font-size: 11px; letter-spacing: 0.14em; text-transform: uppercase; font-weight: 600;" class="text-muted">Manifiesto</div>
    <div style="grid-column: 10 / 13; font-size: 11px; line-height: 1.5; text-align: right;" class="text-muted">Escrito en 2026.<br>Revisión continua.</div>
  </div>

  <h2 style="margin: clamp(28px, 5vw, 64px) 0 0; font-size: clamp(34px, 6.6vw, 108px); line-height: 0.94; letter-spacing: -0.04em; max-width: 17ch;">
    No creemos que el diseño termine donde empieza el <span style="color: var(--ac); font-style: italic; font-weight: 400;">código.</span>
  </h2>

  <div style="display: grid; grid-template-columns: repeat(12, minmax(0, 1fr)); gap: clamp(20px, 3vw, 40px) clamp(8px, 1.6vw, 22px); margin-top: clamp(28px, 5vw, 70px);">
    <div style="grid-column: 1 / 4; border-top: 2px solid var(--ac); padding-top: 10px; font-size: 11px; line-height: 1.5; letter-spacing: 0.04em; text-transform: uppercase; font-weight: 600;">Nota 01<br><span class="text-muted" style="text-transform: none; letter-spacing: 0; font-weight: 400;">Bonito y funcional no son lo mismo. La mayoría de los sitios corporativos los trata como si lo fueran.</span></div>
    <div style="grid-column: 5 / 10;">
      <p style="font-size: clamp(17px, 1.8vw, 26px); line-height: 1.4; margin: 0 0 18px;">Los mejores productos digitales empiezan con mejores preguntas de diseño. Los estudios que se destacan no son los que tienen más filtros — son los que entienden el problema antes de abrir el editor.</p>
      <p style="font-size: clamp(14px, 1.2vw, 17px); line-height: 1.55; margin: 0; max-width: 52ch;" class="text-muted">Trabajamos en el cruce entre lo que una marca necesita comunicar y lo que su sitio realmente permite hacer. Ahí es donde vive el problema real.</p>
    </div>
    <div style="grid-column: 11 / 13; display: flex; justify-content: flex-end;">
      <div style="writing-mode: vertical-rl; font-size: 11px; letter-spacing: 0.3em; text-transform: uppercase; font-weight: 600; color: var(--ac);">Pensar antes de tipear</div>
    </div>
  </div>
</section>
